# Deployment-only credentials. Copy to .cloudflare.env for local provisioning.
# Do NOT add these values to .dev.vars, .env.example, Wrangler config, or source code.
CLOUDFLARE_ACCOUNT_ID=REPLACE_WITH_ACCOUNT_ID
CLOUDFLARE_API_TOKEN=REPLACE_WITH_SCOPED_API_TOKEN
